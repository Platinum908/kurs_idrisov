<Page x:Class="kurs_idrisov.View.BolezniPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BolezniPage">

    <Grid Background="Wheat">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding PriemList}" AutoGenerateColumns="False" Margin="20" Background="#e0c470">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Диагноз" Binding="{Binding Diagnoz}" />
                <DataGridTextColumn Header="Пациент" Binding="{Binding PatientName}" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Content="Поиск" Command="{Binding FilterByDiagnozCommand}" Margin="10"/>
            <TextBox Grid.Column="1" Margin="10" FontSize="20" Text="{Binding SelectedDiagnoz}" />
        </Grid>
    </Grid>
</Page>
